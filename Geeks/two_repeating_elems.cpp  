#include <iostream>
#include <cstdlib>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

void two_repeating_elements(int n,int arr[]){

  int xr = arr[0];
  for(int i = 1;i < n;i++){
               xr ^= arr[i];
  }

}


int main( ){

  int n;
  cin>>n;

  int arr[n];

  for(int i = 0;i < n;i++)
               cin>>arr[i];

  two_repeating_elements(n,arr);

  return 0;
}
