#include <iostream>
#include <string>
#include <cstring>
#include <vector>
#include <cstdlib>
#include <ctypes>


using namespace std;

vector<int> arr(26);

void two_chars(string s){

  for(int i = 0;i < s.length();i++){
               arr[(int)s[i] - 65]++;
  }

  for(int i = 0;i < 25;i++){
    for(int j = i+1;j < 26;j++){
      w
  
  
  

  

int main(){

  int n;
  cin>>n;
  
  string s;

  cin>>s;

  two_chars(s);
  
  return 0;
}